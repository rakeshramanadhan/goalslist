/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Replace with your remote image domain
      },

};

export default nextConfig;
